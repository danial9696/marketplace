/* This code is exporting several components from different files. Each export statement is using the
`default as` syntax to give the exported component a new name that can be used when importing it in
other files. The comments above each export statement are providing a brief description of the
component being exported. */
// * Checkbox component
export { default as Checkbox } from './Checkbox';

// * Primary input component
export { default as PrimaryInput } from './PrimaryInput';

// * Contact input component
export { default as ContactInput } from './ContactInput';

// * Secondary input component
export { default as SecondaryInput } from './ContactInput';

// * Radio component
export { default as Radio } from './Radio';

// * Range component
export { default as Range } from './Range';

// * Select component
export { default as Select } from './Select';

// * Slider component
export { default as Slider } from './Slider';
